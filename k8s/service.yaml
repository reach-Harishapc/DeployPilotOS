apiVersion: v1
kind: Service
metadata:
  name: deploypilot-web-service
  labels:
    app: deploypilot-web
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: deploypilot-web
